# GUIA_DESARROLLO.md — obsoleta

Este archivo describía cómo construir un panel administrativo desde cero en Next.js (`setup.sh` creaba un proyecto nuevo `silbatazo-admin/`). Ese plan **no se ejecutó nunca** (no hay `package.json` en este repo) y se descartó: en su lugar se extendió el sistema real ya en producción (`admin.html` + `veedor.html` + Supabase, sin build step).

El contexto vigente del proyecto está en `CLAUDE.md`. `setup.sh` se deja en el repo sin usar por si se quiere rescatar texto de los prompts, pero no debe ejecutarse.
